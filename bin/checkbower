#!/usr/bin/env node

var checkbower = require('../lib/checkbower.js');
if ( checkbower(process.argv.slice(2)[0]) ) {
  console.log('Valid bower.json found');
} else {
  console.log('Error! Could not validate bower.json');
}
